'use strict';

import chai from 'chai';
//import {LocalStorageDriver} from '../../lib/Driver/LocalStorageDriver';

suite('Driver/LocalStorageDriver', function() {
  test(
    'Class LocalStorageDriver exists in Driver/LocalStorageDriver',
    function() {
      //chai.expect(typeof LocalStorageDriver).to.equal('function');
    }
  );
});